# 设计还原验收

## 2026-08-13 布局修正

- 参考：用户提供的 iPhone 竖屏运行截图。
- 实现：`docs/layout-fixed-phone.png`，393 × 852 运行画布（截图包含设备外框）。
- P1 已修复：顶部标题与系统状态栏争抢空间；标题下移，声音与暂停按钮重新锚定。
- P1 已修复：旧计分牌切图自带数字，动态分数叠加后出现横线和重影；替换为透明空白计分牌。
- P1 已修复：木箱视觉尺寸与 Matter 世界坐标不一致；统一画布缩放、危险线和左右/地板边界映射。
- P2 已修复：当前水果、下一个水果和 HUD 上下间距不均；按 393 × 852 基准重新排布。

## 回归结论

- 标题、控制按钮、HUD、预告框、当前水果、木箱与底部提示牌无重叠或溢出。
- 左侧、中心、右侧落点均落入对应物理坐标；暂停和恢复正常。
- 当前没有未解决的 P0/P1/P2 布局问题。

## 2026-08-13 HUD 二次修正

- 对比图：`docs/hud-before-after.png`。
- P1 已修复：得分和最高分的容器比例与素材比例不一致，导致数值锚点下坠；容器改为素材原始宽高比，数字限定在标签下方的内容槽内。
- P1 已修复：预告框素材自带紫色水果，又叠加运行时水果；替换为 `next-card-empty-v2-alpha.png` 空底板。
- 同屏复核未发现数字越界、双水果重影或 HUD 相互遮挡。

final result: passed

## 2026-08-13 数字字形中心校准

- 得分与最高分从 line box 数学居中改为字形视觉中心校准，统一上移并使用 `em` 基线补偿。
- 水平中心严格使用卡片局部 `50%`；字号、位置和补偿均随容器宽度缩放，不含设备专用像素分支。
- iPhone 与 Pixel 实测数字中心均位于卡片高度约 61.8% 的内容槽中心，跨设备比例误差小于 0.1%。
- 验收截图：`docs/numbers-adaptive-iphone.png`、`docs/numbers-adaptive-pixel.png`。

final result: passed

## 2026-08-13 投放水果遮挡修正

- 待落水果层级提升到木框前景之上，水果轮廓完整可见。
- 只调整投放层级，不改变水平坐标映射、物理落点或 HUD 安全区。

final result: passed

## 2026-08-13 投放水果安全区

- 待落水果从 HUD 行移入木箱投放区，左右移动时不再经过得分牌或“下一个”预告框。
- 水果中心仍按 Matter 336 宽逻辑坐标映射到 329 宽画布，显示位置与真实落点同轴。
- 木箱前景层高于待落水果，木梁会自然遮住边缘；水果底部停在危险线之上。
- 左右极限状态均与计分牌、预告框无矩形相交。

final result: passed

## 2026-08-13 多设备 HUD 自适应

- 预告框裁掉不对称透明边缘，输出 `next-card-empty-v3-alpha.png`，使 DOM 中心与卡片视觉中心一致。
- HUD 尺寸改用手机内容区容器单位 `cqw`，不再依赖桌面视口或仅适配 393 px。
- 数字采用统一局部中心点并补偿字体基线；预告水果采用卡片局部百分比圆心。
- iPhone 393 × 852 验收：`docs/hud-adaptive-iphone.png`。
- Pixel 427 × 952 验收：`docs/hud-adaptive-pixel.png`。
- 两种设备下，得分/最高分水平中心一致，预告水果与预告框水平中心一致，无拉伸和溢出。

final result: passed

## 2026-08-13 三处视觉中心校准

- 得分与最高分数值使用 `left: 50%` 和统一的内容槽中心线定位，不再受文字宽度或百分比边距影响。
- 预告水果使用圆形井中心点定位，并恢复预告框原始宽高比，避免拉伸导致视觉中心偏移。
- 验收截图：`docs/hud-centered-phone.png`。

final result: passed
